{
  "name": "tool_onboarder",
  "tool_type": 2,
  "description": "Script tool that detects one or more CLI binaries on PATH, inspects help output, and writes reusable manifests into tool_manifests.",
  "command_template": [],
  "metadata": {
    "script_path": "scripts/tool_onboarder.py",
    "manifest_required": true,
    "input_contract": {
      "tool_names": "string[]"
    },
    "argument_order": [
      "tool_names"
    ],
    "usage_hint": "Use when the user asks to add, install, or register one or more command-line tools so the system can inspect help output and build structured execution metadata.",
    "capabilities": {
      "adds_cli_tools": true,
      "writes_tool_manifests": true,
      "parses_help_output": true,
      "infers_safe_defaults": true,
      "supports_multi_onboard": true
    }
  }
}
